repos:
  - url: 'https://github.com/missedone/multi-git-sync-test.git'
    branch: main
    subPath: bar
    depth: 1
    auth:
      user: private
      accessToken: {{ getEnv "PAT" }}
    destDir: ./out/shallow/multi-git-sync-test
    schedule: '*/1 * * * *'
